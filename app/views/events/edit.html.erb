<h1 class="text-lg leading-6 font-medium text-gray-900">Editing event</h1>
<%= turbo_frame_tag dom_id(@event) do %>
  <%= render "form", event: @event, submit_text: "Update Event" %>
  <%= link_to "Cancel", event_path(@event), class: "mt-3 relative inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-gray-600 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500" %>
<% end %>

<div class="mt-6">
  <%= link_to "👀 Show this event", @event, class: "hover:underline" %>
</div>

<div class="mt-2">
  <%= link_to "🔙 Events", events_path, class: "hover:underline"%>
</div>
